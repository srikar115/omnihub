export const UPSCALE_ROUTES = {
  BASE: 'api/upscale',
  CREATE: '', // POST /api/upscale
  MODELS: 'models', // GET /api/upscale/models
  CALCULATE: 'calculate', // POST /api/upscale/calculate
} as const;
